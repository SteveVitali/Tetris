// These are all the actions that the user can perform during a Tetris game
public enum Action {
    MOVE_LEFT,
    MOVE_RIGHT,
    HOLD,
    HARD_DROP,
    SOFT_DROP,
    ROTATE_CLOCKWISE,
    ROTATE_COUNTER_CLOCKWISE,
    LOCK_MINO,
    TOGGLE_PAUSE
};
